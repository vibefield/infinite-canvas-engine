# infinite-canvas-engine

Third (and intended-final) attempt at a universal infinite-canvas framework — Figma/Apple-Freeform-grade UX, a userland TRIAD (`defineWidget` faces · `defineBehavior` logic+state · `defineTool` input policy; React or R3F widgets usable as node-editor nodes), built on **strata-ecs** (`../strata-ecs`, `@vibecook/strata-ecs`): archetype ECS + reactivity + opt-in Loro-CRDT durable (transactions, gesture-atomic undo) and presence layers.

Prior attempts (read-only reference): `../infinite-canvas` (v1, on reactive-ecs\@0.3.0 — good subsystems, entangled interaction runtime) and its `apps/playground/src/prototype/` (v2, pointer-as-entity — proven decomposition, faked relations). The full audit of what survived examination is in `draft/examined-inventory.md`.

## Repo layout & branch policy

- `docs/` — committed project docs (implementation plan, future ADRs). Normal commits on `main`.
- `draft/` — design docs + architecture diagram. **Tracked ONLY on the `local-dev` branch; NEVER pushed; never committed on `main`** (it's in `.gitignore`; the files stay present-but-untracked in the working tree).
- Design series (in `draft/`): `examined-inventory.md` (the Laws + keep/discard verdicts) → `design-001-data-model.md` (prefab sovereignty, gesture write protocol) → `design-002-frame-contract.md` (sync→tick→publish→notify→reflect; sub-phases; reflectors; package layering) → `design-003-interaction-stack.md` (L0–L4) → `design-004-widget-runtime.md` (planes, islands/compositor, router, nested canvas) → `design-005-userland-api.md` (defineWidget/defineTool, facade, doc kit). 001–005 were adversarially reviewed and are LOCKED; decisions cite evidence — **do not re-litigate a locked decision without new evidence; amend the doc if you do.**
- Later, NOT locked (proposals — read the Status header before building on them): `design-006-nav-transitions.md` (the portal zoom; T1 camera-flight half is as-built) → `design-007-focus-model.md` (widget keyboard-exclusivity + wheel opt-out; **rev 2, AS-BUILT 2026-08-09** — `interaction.keyboard/keyboardEscape`, `data-canvas-keyboard` marker + focus driver, ownership-aware Space, dynamic `wheelCede` + one-tick `WheelHandled` gating BOTH wheel consumers, facade `keymapOverrides`; downstream petitions I1/I4 satisfied) → `design-008-rename-migration.md` (**AS-BUILT 2026-08-09**, petition I5 — `renamedFrom` on defineWidget, gate tombstone aliasing, open-path rename runner + observer-armed zombie sweep; no strata changes needed) → `design-009-behavior-framework.md` (**AS-BUILT 2026-08-15**, M13/ICE 0.6.0 — `defineBehavior`, ICE's SECOND product surface: A-Frame-shaped declarations where `store: durable|runtime|ephemeral` routes data location, sync, undo, write vocabulary, cadence and attachment. §16 records the six places the build corrected the design; read it before changing the differ, the echo memo, or the SPLIT rule). Inbound-petition status lives in `docs/downstream-petitions.md`.

### Draft workflow (snapshotting draft changes)

Edit drafts on `main`'s worktree as usual, then snapshot with:

```sh
sh scripts/snapshot-drafts.sh "local: what changed"
```

**NEVER `git checkout local-dev` while drafts have uncommitted edits** — git silently OVERWRITES ignored-untracked files with the branch's committed versions (edits lost; this bit us once). The script commits to `local-dev` through a temporary index: no branch switch, no clobber. A `.git/hooks/pre-push` hook blocks pushing `local-dev`; do not remove it. Never merge `local-dev` into `main`.

## Non-negotiable engine laws (digest — full list in draft/examined-inventory.md §3)

- All interaction state lives in the world (no closure FSMs/singletons); one input path (adapters only enqueue).
- Sovereignty at prefab level: durable/runtime/ephemeral = spawn path (`tx`/`world`/`eph`); components and tags stay pure; durable cells written live only under a gesture claim, committed as ONE transaction per gesture at `JustEnded`.
- Absolute writes only; structure from systems via `ctx.*` only; phases/sub-phases are the sole ordering contract; `notify()` once per frame; reflectors are post-notify and never write ECS.
- Churny state = tags/value writes, never per-frame component swaps; interaction-rate tags/relations written change-only (stamp-volume hygiene — no longer a correctness requirement since petition 1 landed per-tag/relation observer precision in strata 0.3.0).
- Pure math stays in `kernel` (no ECS/DOM/React imports); coordinate conversions + Y-flip in exactly one module; the spatial index is the only hit path.
- `access.write` declared on every value-writing system from day one (reflectors arm strata enforcement permanently).

## Working here

- pnpm monorepo (Node 24+): packages `kernel`/`core`/`dom`/`react`/`r3f`/`ground`/`devtools` (`@ice/*`, private workspace packages) plus `ice` — the publish bundle that re-exports them as the ONE npm package `@vibecook/ice` with seven entry points (`packages/ice/src/*.ts`; the `@ice/*` scope stays internal). Import walls are dependency-cruiser-enforced and CI-fatal: kernel imports nothing (single named exception: `rbush`); core = strata-ecs + kernel only; chain dom → react → r3f; devtools → core; ground = core + kernel + three (the P0 ground layer: ONE WebGPU canvas — three WebGPURenderer + TSL, WebGL2 fallback — grid/wires/snap-guides passes); nobody imports devtools or ground (react gets the ground layer as an opaque `<InfiniteCanvas ground={ground(...)}>` factory prop; imperative apps register its reflector).
- `apps/` holds the demo/verification apps — `graybox` (frame contract), `pointerlab` (L0–L4), `cardboard` (DOM widgets), `glboard` (GL islands), `nodeboard` (node editor), `moodboard` (collab), `widgetlab` (the full showcase), `widgetlab-desktop` (Electron + truffle mesh). Run one with `pnpm --filter <app> dev`; `pnpm relay` starts the ws relay for `?room=x&relay=ws://localhost:9301`.
- Commands: `pnpm run ci` (typecheck + lint + test + depcruise — the merge gate) · `pnpm run build` · per-package `pnpm --filter @ice/kernel test`.
- strata-ecs pinned at `0.12.0` (pre-1.0; bumped 2026-08-15 — ADDITIVE, petitions 9+10 for the behavior framework: `transaction(fn,{meta})` + `batch.meta` (per-commit provenance; cap enforced at entry, receive side sanitizes, shallow saves erase provenance below the compaction boundary), `valueEquals(c,a,b)` (canonical COMPONENT-cell equality — the design-009 differ imports it, never a hand-rolled one; resources deliberately excluded), `world.resourceStamp(res)` (per-WRITE counter, world-wide arming, `reset()` bumps every held resource). Full CI green at the bump — 812 tests, walls clean. Prior 0.11.0 note kept: `applyRemote` derives from a change's own ops instead of `doc.diff` (~2.5× receive path), and deleting an already-absent key emits a redundant remove, so anything counting batches per commit sees it): API-verify against `../strata-ecs/src` when in doubt (the design docs cite file:line for every load-bearing claim); upgrades re-run the full trace suite — and check ALL package.json declarations incl. `apps/*` (a stale app pin resolves a SECOND strata copy: two schema registries at one seam, which no unit suite catches). 0.3.0–0.9.0 shipped ALL EIGHT engine petitions (`{undoable:false}` tx; observer precision; `orderIndependent` attestation; `metaTransaction`; `devOnWrite`; `defineTickSystem` + attributed `ctx.query` + zero-match chunk skip; pull-based `ChangeCollector` — spatialSync is O(delta) now; public entity introspection; ordered relations — sibling order replaced scalar `StackZ`) — record new ones in `docs/strata-petitions.md` (one file per petition in `docs/petitions/`).
- `loro-crdt` pinned at `^1.13.8` — the version strata 0.11.0–0.12.0 is developed against (its peer range is only `>=1`, so the floor is ours to hold). 1.13.8 makes a receive-only peer's import dramatically cheaper (61 ms → 3.9 ms at 10k entities); an actively-editing peer sees no benefit, since that path is bounded by Loro reconciling against concurrent local history. Keep exactly ONE resolved loro copy — two produce `expected instance of LoroDoc` at a package seam, which no unit suite catches. After any bump assert it against the LOCKFILE (`grep -o 'loro-crdt@1\.[0-9.]*' pnpm-lock.yaml | sort -u` must print one line — that string also carries strata's peer-resolution key, so it proves strata and core share the copy). Do NOT check by listing `node_modules/.pnpm`: it keeps unlinked entries from earlier installs and will show versions nothing resolves to.
- Milestones + exit criteria: `docs/implementation-plan.md` — **M0–M13 are all complete** (engine v1 at M10; the post-v1 behavior-framework train M11–M13 shipped 2026-08-15) (kernel, spine, interaction stack, durable docs + per-gesture undo, DOM runtime, GL islands, node editor, nested canvas, presence/bootstrap/migrations, facade, devtools). New work is post-v1: hold the design-005 §9 scope fence (no layout engine, no rich-text, no comments, no permissions — each has a named seam instead).
- Verify design claims against the docs before "fixing" behavior that looks odd — several counterintuitive choices (e.g., ports on-demand, stratified z, OS cursor default) are deliberate, reviewed decisions.
