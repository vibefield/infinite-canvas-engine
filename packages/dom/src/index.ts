/**
 * @ice/dom — DOM planes, pointer adapters (events→queue ONLY), built-in DOM reflectors.
 * Import wall: @ice/core down only — never react/three (enforced).
 */
export const DOM_VERSION = "0.0.0";
