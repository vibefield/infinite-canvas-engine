/**
 * @ice/devtools — strata observer panel extensions: pointers/recognizers, planes,
 * sovereignty badges, loop telemetry. Import wall: @ice/core (+kernel) only; nobody imports us.
 */
export const DEVTOOLS_VERSION = "0.0.0";
