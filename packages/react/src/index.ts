/**
 * @ice/react — <InfiniteCanvas>, hooks, widget slots/island bridge.
 * Import wall: @ice/dom down only — never three/@react-three (enforced). react is a peer.
 */
export const REACT_VERSION = "0.0.0";
