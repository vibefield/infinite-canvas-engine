/**
 * @ice/r3f — R3F islands, virtual-texture compositor, GL renderers (grid/wires/SDF).
 * The ONLY package that touches three/@react-three (as peers). Top of the chain.
 */
export const R3F_VERSION = "0.0.0";
