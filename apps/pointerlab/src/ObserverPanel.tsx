/** STUB — full 1:1 port of v2 ObserverPanel.tsx + EntityTimeline.tsx lands via task 59. */
import type { Engine, World } from "@ice/core";

export function ObserverPanel(_props: { world: World; engine: Engine }) {
  return null;
}
