# Downstream petitions (inbound)

Asks filed AGAINST this engine by its consumers — the inbound twin of
`docs/strata-petitions.md` (which records what ice asks OF strata). Before this
file existed, inbound asks lived only in the consumer's repo and were invisible
from here; I5 sat unrecorded for 17 days. House rules mirror the outbound ones:
evidence with file:line, field impact stated, additive-first, workaround
retirement recorded. Source of truth for the full petition texts stays the
consumer's repo (VibeField: `vibe-field/draft/petitions/`, I-series).

| ID | From | Title | Status |
|---|---|---|---|
| I1 | VibeField | Widget input-exclusivity — `defaultPrevented` gating + keyboard claim | **IMPLEMENTED 2026-08-09** — design-007 rev 2 as-built: `interaction.keyboard: "shared" \| "exclusive"` + `keyboardEscape: "release" \| "widget"`, `data-canvas-keyboard` marker + focus driver (`attachWidgetFocus`, `InfiniteCanvasHandle.focus`), keymap/adapter standdown behind ONE shared predicate family, Stage-1 `defaultPrevented` gate, facade `keymapOverrides` (the widgetlab C-key capture-phase hack is deleted — the migration proof). Undeclared widgets byte-identical. |
| I4 | VibeField | Wheel opt-out for scrollable widget content | **IMPLEMENTED 2026-08-09** — rides the same landing: dynamic `wheelCede` (scroller-with-room inside claim/`data-canvas-interactive`/editable subtrees; at-bounds falls through to canvas — nested-scroll feel), ctrl/pinch ALWAYS canvas zoom, one-tick `WheelHandled` gating BOTH wheel consumers (spawn `Not(WheelHandled)` + feed-as-silence — the feed gate was found at implementation; rev 1's spawn-only fix was insufficient). The wrapper `stopPropagation('wheel')` rule retires; unmodified `overflow:auto` content (mille's tree) scrolls natively. |
| I5 | VibeField | First-class prefab RENAME migration (type → type) | **IMPLEMENTED 2026-08-09** — design-008 as-built (same-day design + build): `renamedFrom` on `defineWidget`, gate aliasing + `engine.renamed.<old>` tombstones (un-bricks the readOnly classification; meta cannot delete, so old markers are skipped, never removed), the open-path runner under the single-writer law (fold → tombstone + carried `engine.pack.<new>.<oldV>` marker → the M9 chain composes on new names), and the observer-armed zombie sweep on every writable session (resurrections drop new-wins, late old-shape entities fold + chain from `atVersion`, id-zombies rewrite). **No strata petition was needed** — see below. VibeField's offline wrapper retires on adoption. |
| I14 | VibeField | `engine.guests` — first-class guest-system runtime at the publish seam | **IMPLEMENTED 2026-08-15 (M11)** — `engine.guests.add({id, make, budgetMs?, phase?, ledger?})` in the named frame slot (`tick → guests → publish → notify → reflect`, design-002 §1.1 as-built); the ED-D3 breaker built once (trip ladder, warmup exemption, thenable rejection, 8ms seam cap, dev leniency keyed to `enableTelemetry`, HOST-INJECTABLE ledger + `onLedgerChange`); snapshot iteration; `settleWhile("guests")` fed by an optional `busy()`; `guests.list()` + per-guest devtools lanes; `EngineOpts.onGuestFault/onGuestNotice`. **All three standing bugs fixed with it**: the rAF loop now survives a throwing step (it died permanently before — and the fix also closed a latent stop()-during-step resurrection), the publish-hook AND reflector loops no longer skip neighbours on self-removal, and the devtools "reflect" lane reports for the first time (it read a phase key that never existed; `FrameTelemetry.reflectMicros`/`reflectorMicros` are the real source). 44 traces, each mutation-probed. Was: **DRAFTED 2026-08-13** (plugin ECS door rev 3 §7-U1; awaiting James's bless). Cadence-extensible `guests.add({id, make, budgetMs?, phase?, ledger?})` (the door's ED-D5 factory shape verbatim — the SDK never changes); a NAMED guest sub-step in the frame contract (design-002 §1 amendment: `tick → guests → publish hooks`, snapshotted list); the ED-D3 circuit breaker built once with dev leniency and a HOST-INJECTABLE ledger (engines are per-doc — an ICE-internal ledger would reset per generation); `settleWhile("guest-derive")`; per-guest devtools lanes + `guests.list()` introspection. Carries three standing-bug fixes found by the rev-3 review regardless of guests: a throwing publish hook PERMANENTLY KILLS the DOM rAF loop (`loop.ts:28-38` reschedules only after step); the hook loop AND `reflectors.flushAll()` iterate live arrays (splice-skips a neighbor); the devtools "reflect" lane reads a nonexistent phase name and has never reported. Full text: `vibe-field/draft/petitions/I14-ice-guest-runtime.md`. |
| I15 | VibeField | `GuardedTx.move` — animation-integrated writes + the tween's external-write hole | **IMPLEMENTED 2026-08-15 (M12)** — `tx.move(e, to, {animateMs})` on `GuardedTx`: capture-at-call, durable final in THIS tx (inheriting `undoable`), tween + guarded rewind after the seal; retarget-not-restart; Grab-skip; `animateMs: 0` on an in-flight glide ENDS it (write-then-remove — a stale tween must not fight the snap). **The permanent-divergence hole is closed at both chokepoints**: post-seal in `guardedTransaction` (a Position write that did not come through `move` retargets the tween) and post-history in the facade (undo/redo retarget onto doc truth — history never passes through the tx path), so landing always reconverges. `ops.arrange` is now a pass-through and RETARGETS in-flight movers instead of skipping them; `tweenStart` reaped on destroy/reset; `makeChurnGuard` exported. design-001 §3 carries the erratum. Was: **DRAFTED 2026-08-13** (door rev 3 §7-U2/ED-D9; awaiting James's bless). Upstreams arrange.ts's own recipe (capture-at-call → final-in-tx → tween+rewind; retarget-not-skip) onto `GuardedTx` proper — and closes a REAL divergence bug the review traced: local undo mid-glide takes strata's `(own, value)` branch (baseline advances, nothing banked), the tween lands on the stale target, and the cell strands PERMANENTLY, poisoned against later remote updates (remote-mid-glide self-heals via the held-cell ledger; local undo has no such luck). Fix = two ICE-only chokepoints: guardedTransaction post-seal retarget sweep + facade undo/redo retarget sweep. Also: arrange reconciles onto retarget (its skip-filter strands a first glide's targets), `tweenStart` leak reaped, `makeChurnGuard` exported, the write protocol externalized from the op header into a documented contract. Full text: `vibe-field/draft/petitions/I15-ice-tx-move.md`. |
| I16 | VibeField | Complete the `defineBehavior` host contract — order, ledger seed, facade diagnostics, `describeBehavior`, thenable faults | **IMPLEMENTED 2026-08-16 (0.7.0)** — `engine.behaviors.register(B, {orderKey?, ledger?})` (lexical keyed lane BEFORE unkeyed; ties/unkeyed keep registration order; same order for pipeline systems AND publish-slot hooks; reorder reinstalls EXECUTION only — collectors/instances/guests/data stay put, `init` does not re-run, pinned by test; empty key throws); `ledger` seeds the driven guest so suspensions survive engine generations, streaming out the existing `guests.onLedgerChange`; `createCanvasEngine({onGuestFault?, onGuestNotice?, onBehaviorFault?, onBehaviorLog?})` forwards the routes that already existed beneath the facade with hook+entity provenance; `describeBehavior(B)` — canonical JSON-safe process-independent projection (schema/reads/writes/migrations/hooks; no function identity, no generated ids) as the ONE definition-identity surface for manifest emission + anti-drift; thenable hook returns = attributed faults with a catch observer, escalated straight to the guest strike ladder (an async hook is a definition bug; ordinary single-instance throws stay instance-quarantined), `dispose` thenables detected-but-swallowed (teardown must not stop teardown). 9 acceptance tests (the petition's six controls + keyed/unkeyed lane, empty-key refusal leaves no residue, async-dispose teardown). Was: **DRAFTED + EXECUTABLY PROVEN 2026-08-16** from PRC-4 — released-0.6 gaps pinned by ten controls against the installed registry artifact; an isolated candidate patch moved the contract 5/5 red → 6/6 green preserving 546/546 core tests; the landing re-derived that patch on main. Full text: `vibe-field/draft/petitions/I16-ice-behavior-host-contract.md`. |
| I17 | VibeField | Withdraw ephemeral behavior facets on unregister, suspension, and singleton quarantine | **IMPLEMENTED 2026-08-16 (0.7.0, with I16)** — one idempotent `withdrawFacet()` owned by the behavior NODE, invoked on every producer-stops edge: unregister/disposal (in `endGeneration`, after the dispose hooks, before ownership is forgotten — a world-reset generation end is a quiet no-op, the peer is already dead), guest suspension (`goCold`, beside the collector detach — resume remints defaults through the ordinary `ensureFacet` path), and singleton quarantine (the edge with NO guest-ledger transition a host could watch — a singleton's throws cannot span instances). Removal goes through the PRESENCE writer so remote projections get tombstone truth, pinned by a recording-delegate test. One trap found past the petition: withdrawal makes the quarantined instance DEPART next deliver, so an instance-keyed quarantine check would forget and re-mint into an infinite quarantine/remint oscillation that never strikes the guest — the memo lives on the NODE (`singletonQuarantined`), resets with the generation, and resume deliberately does NOT re-mint a quarantined facet (fresh registration does). 5 tests incl. the ledger-seeded suspended-at-birth case. Was: **DRAFTED 2026-08-16 (PRC4-E12)** — six controls proving the stale-publication bug on the patched package; no candidate patch was claimed. Full text: `vibe-field/draft/petitions/I17-ice-ephemeral-behavior-withdrawal.md`. |
| I18 | VibeField | Attach facade presence after `docs.create/open` without adopting ICE document bootstrap | **IMPLEMENTED 2026-08-16 (0.8.0, M15)** — `docs.attachPresence(opts): () => void`, the petition's minimum ask verbatim: session gate + duplicate gate, the existing `attachPresence(world, opts)`/`installPresence` pair under the hood, seam exposure through `docs.presence()` (which the behavior runtime's per-publish forwarding reads — a registered ephemeral behavior leaves dormancy on the next publish step, one facet, init once), idempotent IDENTITY-BOUND inverse (a stale inverse cannot detach a replacement), auto-teardown from `docs.close()` and disposal, leave tombstones flushed through still-subscribed outbound before wiring dies (best-effort per subscriber — a throwing transport forfeits its own delivery, peers fall back to TTL; `PresenceOpts.onFault` observes), reattach remints a fresh peer + behavior DEFAULTS. `join({presence})` rides the same internal acquisition, framing unchanged. **One trap found past the petition**: the inverse detaches presence on a STILL-OPEN document, which strands the remote-cursor derive pool as ghost cursors (the join path never saw it — `close()`'s world reset hid the strand); `installPresence`'s uninstall now reaps the pool, and the system moved to the `present` phase (no in-tick consumers) to stop tripping strata's access advisories against `cull`/`selectionChrome` on presence-attached facade engines. 8 acceptance tests (`facade-presence.test.ts`). Was: **DRAFTED 2026-08-16 (PRC4-E13) — OPEN, P2.** Controls 7/7: create/open plus standalone `attachPresence(world)` leave behavior-facade presence dormant, while the identical intent activates through `docs.join({presence})`. Bounded ask, no patch claimed. Explicitly NOT a gate for durable/runtime plugin behaviors — it unlocks the ephemeral profile for hosts that own their document lifecycle. Full text: `vibe-field/draft/petitions/I18-ice-split-presence-attach.md`. |
| I19 | VibeField | Enforce a declared byte budget for ephemeral behavior facets | **IMPLEMENTED 2026-08-17 (unreleased)** — `maxFacetBytes` on `defineBehavior` (ephemeral only, optional), the petition’s minimum ask verbatim: canonical UTF-8 JSON bytes of the COMPLETE facet cell after defaults/merge/serialization (schema declaration order IS the cell construction order, so plain `JSON.stringify` is canonical — not a wire-frame guess; the host budgets encoding/transport headroom and keeps its independent cap). PRODUCTION enforcement at both mint paths: an over-budget DEFAULT fails `defineBehavior` itself (unconditionally, outside the dev-guard gate — no residue), an over-budget `ctx.write` throws BEFORE mutation with the prior facet intact — in-hook that feeds the existing ladder (per-entity attribution, BF-D18 three strikes, I17 withdrawal on quarantine; NOTE for the host: a singleton’s strikes are instance quarantine, not persisted guest-ledger entries — the deliberate I17 semantic), and the captured-closure path gets the same refusal synchronously. Identity-bearing: in the definition signature (claim-only change = DIFFERENT shape) and `describeBehavior()` (absent = no bound attested — hosts can refuse unattested rows). Beyond the ask, the round’s store-routing audit closed four holes: `engine.behaviors.attach`/`detach` + `tx.attach`/`tx.detach` accepted ephemeral behaviors and world-wrote the facet component (a local `ctx.peers()` remote-facet spoof; never published, never withdrawn) — all four refuse now, so the facet’s only writers are `ensureFacet` and `ctx.write`, both presence-routed and both measured. 14 acceptance tests (`behavior-facet-budget.test.ts`), every enforcement point mutation-probed. Was: **DRAFTED 2026-08-17 (PRC4-E22), OPEN P1** for unrestricted plugin ephemeral admission; not a gate for the core document-room transport or durable/runtime behaviors. Full text: `vibe-field/draft/petitions/I19-ice-ephemeral-facet-budget.md`. |

## The behavior framework (M13) — no new petitions

design-009 shipped 2026-08-15 as ICE 0.6.0 compiled entirely onto the substrate I14 and
I15 landed, exactly as BF-D12 predicted: `engine.guests` gained `addDriven` (the breaker
detached from the scheduler — behaviors run as pipeline systems inside the tick, so
nothing in the registry can call them, but they still share one ledger, one doctor row
and one seam total), and `GuardedTxOpts` gained `meta` (ICE's first consumption of strata
petition 9 — every `ctx.commit` stamps `{behavior, label}` provenance). Both additive.
Nothing upstream was needed; strata 0.12.0's `valueEquals` + `resourceStamp` were already
in hand from petitions 9+10.

## I5 — prefab rename migration (resolved)

**The ask** (full text: `vibe-field/draft/petitions/I5-ice-rename-migration.md`):
an in-band, engine-owned TYPE rename in the M9 family, running under the
single-writer law through ordinary `store.transaction()` traffic, stamping pack
markers and a tombstone so replicas converge through normal CRDT delivery —
retiring VibeField's offline pre-attach Loro surgery (sound only
single-replica).

**Resolution (design-008, as-built same day):** the feared blocker — strata
never projects unknown component names, so the old `comp:<old>:<group>` cells
looked unreachable in-band — dissolved on source verification: projection is
NAME-REGISTRY-driven (strata `componentsByName`), not prefab-driven, so
`renamedFrom` simply registers same-shaped LEGACY components under the old
names and the old cells project like any other value. No raw-cell ops, **no
strata petition #9**. The convergence half landed as designed: the tombstone's
real job is the deterministic engine-level sweep (an `observeQuery` +
between-frames microtask fold on every writable peer); "zero zombies" is a
sweep property, not a CRDT freebie, and the one true-conflict case (a stale
edit resurrecting a cell beside a post-rename cell) resolves NEW-WINS —
deterministic, and the only rule that never clobbers post-rename work.

Carried limitations (design-008 §9): read-only pre-rename docs render nothing
for old types until a writable open folds them; `definePrefab`-direct ids and
group regrouping across a rename keep their named seams.
